# v0.6.0 superseded release note

Add this note to the GitHub Release page for `v0.6.0`:

```markdown
> Superseded by v0.6.1. Use v0.6.1 or later for the corrected SARIF upload workflow.
```

Do not delete or move the `v0.6.0` tag unless you intentionally want to rewrite release history.
