# v0.8.0 milestone: Rule precision and packaging polish

Current focus after v0.7.0:

## Rule precision

- Refine context handling for package-manager cache findings.
- Refine Docker Compose project-name checks.
- Refine GitHub Actions cache-key specificity checks with fewer false positives.
- Add rule tests for common false-positive examples.
- Validate publish-ready rule catalog metadata and regex syntax.

## Packaging and release quality

- Expand release smoke tests across all published binaries.
- Keep embedded default rules synchronized with the repository rule catalog.
- Add checksum verification examples to install docs.
- Consider publishing package-manager installation paths such as Homebrew or Scoop.

## Documentation

- Expand baseline adoption examples.
- Document the rule authoring workflow.
- Add a contributor guide section for adding and testing rules.

## GitHub Action

- Support multiple rule IDs through a comma-delimited action input.
- Document artifact upload and SARIF upload in the same workflow.
